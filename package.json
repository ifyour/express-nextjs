{
  "name": "express-nextjs",
  "version": "1.0.0",
  "description": "Deploying a Next.js app with an Express Backend.",
  "main": "index.js",
  "scripts": {
    "start": "node ./server/index.js",
    "dev": "nodemon ./server/index.js --watch server",
    "build": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
